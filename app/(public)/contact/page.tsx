import { Contact } from '@/features/contact/contact';

export const metadata = {
  title: 'Contact',
  description:
    'Get in touch for art direction and styling collaborations, editorial projects, and creative inquiries.',
};

export default function ContactPage() {
  return <Contact />;
}
